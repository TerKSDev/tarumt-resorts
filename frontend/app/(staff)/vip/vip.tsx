import { type MetaFunction } from "react-router";

export const meta: MetaFunction = () => [
  { title: "VIP Priority Queue | TARUMT Resorts" },
];

export default function VIPPriorityQueue() {
  return <main className="flex-1 min-h-screen">VIPPriorityQueue</main>;
}
